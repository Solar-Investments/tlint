# TLint

Custom TLint preset.

## Installation

Install the package:

```bash
composer require --dev solar-investments/tlint
```

Create a configuration file:

```bash
touch tlint.yml
```

Configure the preset:

```yaml
{
  preset: default
}
```
